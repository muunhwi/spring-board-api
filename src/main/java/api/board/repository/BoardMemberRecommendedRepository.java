package api.board.repository;

import api.board.object.board.BoardMemberRecommended;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardMemberRecommendedRepository extends JpaRepository<BoardMemberRecommended, Long> {

}
